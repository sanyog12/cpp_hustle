#include <iostream>
using namespace std;

int main() 
{
	cout<<"Enter the no x & y"
    int x, y;
    cin>>x;
	cin>>y;
    cout << "Sum is  = " << (x + y) << endl;
    cout << "Substraction is = " << (x - y) << endl;
    cout << "Multiplication is  = " << (x * y) << endl;
    cout << "Division is = " << (x / y) << endl;
    cout << "Modulus is = " << (x % y) << endl;
    
    return 0;
}
